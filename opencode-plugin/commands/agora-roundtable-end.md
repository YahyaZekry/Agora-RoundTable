---
description: End the active roundtable session and return to normal.
agent: agora-facilitator
subtask: true
---
End the active roundtable. Delete roundtable-session.json and drop all coach personas. Confirm in one line; personas stay saved. If no session file exists, say there's no active roundtable.
