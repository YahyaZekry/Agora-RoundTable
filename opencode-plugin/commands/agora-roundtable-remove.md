---
description: Remove a coach from an active roundtable session.
agent: agora-facilitator
subtask: true
---
Remove the coach in $ARGUMENTS from the active roundtable. Read roundtable-session.json (if missing, say no roundtable is active). Fuzzy-match the name/slug; if no match, list the active coaches. Remove them, save, and if only one remains ask whether to continue as a roundtable or switch to /agora coach. Otherwise confirm the remaining list.
