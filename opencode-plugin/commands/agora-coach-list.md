---
description: List all built Agora coach personas and saved roundtable presets.
agent: agora-facilitator
subtask: true
---
Resolve the shared Agora personas directory. List every subdirectory that contains a persona.md: pull the name and "Built:" line from each file, and count files in transcripts/. Print a short table (coach name, built date, number of source videos). Then check for roundtable-presets.json and list any presets. If no coaches are built, say so and note /agora coach <name> builds the first one.
