---
description: Sync inbox/ for every built coach, or every coach in a named preset.
agent: agora-facilitator
subtask: true
---
Run the inbox sync (per the coach-update command) for every built coach, or if $ARGUMENTS is a single token matching a preset in roundtable-presets.json, only that preset's coaches. Label each coach as you go and print a compact summary at the end.
