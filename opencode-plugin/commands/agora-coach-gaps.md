---
description: Ask a coach where their own knowledge is thin, or show gaps already recorded.
agent: agora-facilitator
subtask: true
---
Audit where a coach's sourcing is thin. Resolve the shared data dir and identify the coach from $ARGUMENTS (or the active session, or survey mode if empty). First show any open entries in PERSONAS/<slug>/research/_gaps.md. Then spawn the persona as a subagent, step OUT of character, and have it audit its own research/ honestly: what's missing, what's too thin, and what specific source would close each gap. Classify each gap (unmined → extract from transcripts/; unresearched → researchable; user-only → drop in inbox/). Append to _gaps.md and report grouped by class.
